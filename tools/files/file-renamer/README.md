# File Renamer (sample)

Demo tool: prompts for a directory and lists files inside it. It does **not** rename or delete anything.

## Run

```bash
bash tools/files/file-renamer/file-renamer.sh
```

Or use `./start.sh` → Run tool.
