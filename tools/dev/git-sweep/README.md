# Git Sweep (sample)

Demo tool: checks that the current directory is inside a git work tree, then lists **local** branch names. It does not delete branches or change git state.

## Run

From any directory inside a repo:

```bash
bash tools/dev/git-sweep/git-sweep.sh
```

Or `./start.sh` → Run tool.
